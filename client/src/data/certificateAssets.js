/**
 * Certificate logo asset — school text comes from GET /api/certificates/meta.
 */

import certificateHeaderLogo from "../assets/images/certificate/header-logo.png";

export const certificateHeaderLogoUrl = certificateHeaderLogo;
